using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestFloat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        double a = 0.02d + 0.08d;

        Debug.Log(a);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
